// Excalidraw ships its stylesheet behind a package export subpath that TS can't
// resolve as a module; the side-effect import is handled by the bundler.
declare module "@excalidraw/excalidraw/index.css"
